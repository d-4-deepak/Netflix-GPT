/** @type {import('tailwindcss').Config} */
const config = {
    theme: {
      extend: {
        screens: {
          xs: '240px',
        },
      },
    },
    variants: {},
    plugins: [],
  };
  
  export default config;
  